function solve(number) {
    number = Number(number);
    if (number < 100 && number != 0)
        console.log("invalid");
    else if (number > 200 && number != 0)
        console.log("invalid");

}
solve(0);